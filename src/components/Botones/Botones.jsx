const Botones = ({text}) => {
    return(
        <button className="bg-verdePrincipal text-colorBgNavbar hover:bg-colorBgNavbar
         hover:text-verdePrincipal border-2 border-verdePrincipal" type="submit">{text}</button>
    )
    
}
export default Botones
